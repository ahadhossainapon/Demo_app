<p id="notice"><%= notice %></p>

<h1>Subject Of this Teacher</h1>

<table>
  <thead>
  <tr>
    <th>name</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @teacher.subjects.each do |subject| %>
      <tr>
        <td><%= subject.name %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<%= link_to "add subject",  add_subjects_teacher_path %>